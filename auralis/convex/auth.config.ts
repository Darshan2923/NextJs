// https://easy-clam-93.clerk.accounts.dev

const authConfig = {
    providers: [
        {
            domain: "https://easy-clam-93.clerk.accounts.dev",
            applicationID: "convex",
        }
    ]
}

export default authConfig;